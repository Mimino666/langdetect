language: python
python:
  - "2.7"
  - "3.2"
  - "3.3"
  - "pypy"
install:
  - "pip install ."
  - "pip install -r requirements.txt"
script: python -m unittest discover
branches:
  only:
    - master
